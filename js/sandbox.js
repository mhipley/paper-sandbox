// Create a raster item using the image tag with id='mona'
var raster = new Raster('sample');

// Move the raster to the center of the view
raster.position = view.center;

// Scale the raster by 50%
raster.scale(0.5);
